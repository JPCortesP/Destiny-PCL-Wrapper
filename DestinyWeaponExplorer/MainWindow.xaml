<Window x:Class="DestinyWeaponExplorer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DestinyWeaponExplorer"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="5*"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.ColumnSpan="3">
            <TextBlock Text="{Binding GamerTag}"></TextBlock>
            <TextBlock Text="{Binding type}"></TextBlock>
        </StackPanel>
        <ListView Grid.Row="1" ItemsSource="{Binding Characters}" 
                  ScrollViewer.HorizontalScrollBarVisibility="Visible"
                  SelectionMode="Single" MaxHeight="800">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Focusable" Value="false"/>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" ScrollViewer.CanContentScroll="False"></StackPanel>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Width="300">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Class}"></TextBlock>
                        </StackPanel>
                        
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Race}"></TextBlock>
                            <TextBlock Text="{Binding Gender}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding BaseLevel}"></TextBlock>
                            <TextBlock Text="{Binding LightLevel}" />
                        </StackPanel>
                        <ScrollViewer>
                            <ListView ItemsSource="{Binding Items}">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Image Source="{Binding Icon}" />
                                            <StackPanel>
                                                <TextBlock Text="{Binding itemName}" />
                                                <TextBlock Text="{Binding itemTypeName}" TextWrapping="Wrap" />
                                                <TextBlock Text="{Binding itemDescription}" />
                                            </StackPanel>
                                            
                                        </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                        </ScrollViewer>
                        
                        
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
    </Grid>
</Window>
